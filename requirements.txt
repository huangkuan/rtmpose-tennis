# Known-compatible development stack (Python 3.11).
# Keep NumPy below 2 for compatibility with this PyTorch/OpenMMLab generation.
numpy>=1.24,<2
torch==2.1.2
torchvision==0.16.2
opencv-python>=4.8,<5

# OpenMMLab 1.x stack used by RTMPose.
mmengine>=0.8,<1
mmcv>=2.0.1,<2.2.0
mmdet>=3.2,<4
mmpose>=1.3,<2

# Installation helper; use `mim install` for mmengine/mmcv when pip cannot find
# a wheel matching the local PyTorch/platform combination.
openmim==0.3.9
